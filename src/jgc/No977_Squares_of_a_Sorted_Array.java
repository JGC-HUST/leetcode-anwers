package jgc;

import java.util.Arrays;

public class No977_Squares_of_a_Sorted_Array {
    public int[] sortedSquares(int[] A) {
        for (int i = 0; i < A.length; i++) {
            A[i] = A[i] * A[i];
        }
        Arrays.sort(A);
        return A;
    }
}
